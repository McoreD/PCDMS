<Window x:Class="PCDMS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCDMS"
        mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <StackPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="New _Word document" Tag="Word document" Click="MenuItem_Click" />
                <MenuItem Header="New _Excel workbook" Tag="Excel workbook" Click="MenuItem_Click"/>
                <MenuItem Header="New _PowerPoint presentation" Tag="PowerPoint presentation" Click="MenuItem_Click"/>
                <Separator />
                <MenuItem Header="E_xit" />
            </MenuItem>
        </Menu>
        <DataGrid x:Name="dgDocuments"></DataGrid>
    </StackPanel>
</Window>
